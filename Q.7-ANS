Q7. Use Array.reduce() method to reverse the following given array
const arr = [1, 2, 3, 4, 5]

ANS:
To reverse the given array [1, 2, 3, 4, 5] using Array.reduce():
const arr = [1, 2, 3, 4, 5];
const reversedArray = arr.reduce((reversed, current) => [current, ...reversed], []);
console.log(reversedArray); // Output: [5, 4, 3, 2, 1]
In this code, Array.reduce() iterates through the original array and constructs a new array in reverse order by adding each element to the beginning of the accumulator array (reversed) using the spread operator (...). The result is the reversed array [5, 4, 3, 2, 1].
